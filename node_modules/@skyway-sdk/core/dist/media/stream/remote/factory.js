"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createRemoteStream = void 0;
const common_1 = require("@skyway-sdk/common");
const audio_1 = require("./audio");
const data_1 = require("./data");
const video_1 = require("./video");
/**@internal */
const createRemoteStream = (id, label, media) => {
    if (media instanceof RTCDataChannel) {
        return new data_1.RemoteDataStream(id, label, media);
    }
    else {
        if (media.kind === 'audio') {
            return new audio_1.RemoteAudioStream(id, label, media);
        }
        else if (media.kind === 'video') {
            return new video_1.RemoteVideoStream(id, label, media);
        }
    }
    throw new common_1.SkyWayError({
        type: 'notImplemented',
        message: 'invalid stream type',
    });
};
exports.createRemoteStream = createRemoteStream;
//# sourceMappingURL=factory.js.map