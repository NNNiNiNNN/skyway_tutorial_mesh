import { Event } from '@skyway-sdk/common';
import { ContentType, Stream } from '..';
import { LocalAudioStream } from './audio';
import { LocalDataStream } from './data';
import { LocalVideoStream } from './video';
/**@internal */
export declare abstract class LocalStreamBase implements Stream {
    readonly label: string;
    readonly contentType: ContentType;
    readonly side = "local";
    readonly id: string;
    private _muted;
    /**@private */
    readonly _onMuted: Event<void>;
    /**@private */
    readonly _onUnmuted: Event<void>;
    constructor(label: string, contentType: ContentType);
    get muted(): boolean;
    mute(): void;
    unmute(): void;
}
export declare type LocalStream = LocalAudioStream | LocalVideoStream | LocalDataStream;
export interface LocalPlayableStream {
    /**@private */
    readonly _onReplaceTrack: Event<MediaStreamTrack>;
    /**
     * @description {japanese} Video|Audio Streamの場合、trackを入れ替える
     */
    replaceTrack: (track: MediaStreamTrack) => void;
}
//# sourceMappingURL=index.d.ts.map