import { SkyWayConnection } from '../plugin/interface';
import { Member, MemberImpl } from '.';
export interface RemoteMember extends Member {
    readonly side: 'remote';
}
/**@internal */
export interface RemoteMemberImpl extends MemberImpl {
    readonly side: 'remote';
    _getConnection: (localPersonId: string) => SkyWayConnection | undefined;
    _getOrCreateConnection: (localPersonId: string) => SkyWayConnection;
}
/**@internal */
export declare function isRemoteMember(member: Member): member is RemoteMemberImpl;
//# sourceMappingURL=remoteMember.d.ts.map