import { HttpClient } from '@skyway-sdk/common';
import { TurnPolicy } from '../config';
/**@internal */
export declare class IceManager {
    private domain;
    private version;
    private secure;
    turnPolicy: TurnPolicy;
    stunServers: RTCIceServer[];
    turnServer?: RTCIceServer;
    private readonly _endpoint;
    readonly http: HttpClient;
    constructor(domain: string, version: number, secure: boolean, turnPolicy: TurnPolicy);
    setupIceServers(jwt: string, memberId: string, ttl?: number): Promise<void>;
    get iceServers(): RTCIceServer[];
}
export declare type TurnCredential = {
    username: string;
    credential: string;
    ttl: number;
    urls: string[];
};
//# sourceMappingURL=ice.d.ts.map