import { RemoteMemberImpl } from '../../..';
import { SkyWayChannelImpl } from '../../../channel';
import { MemberImpl } from '../../../member';
import { UnknownConnection } from './connection';
import { UnknownPlugin } from './plugin';
export declare class UnknownMemberImpl extends MemberImpl implements RemoteMemberImpl {
    readonly type = "bot";
    readonly subtype: string;
    readonly side = "remote";
    readonly plugin: UnknownPlugin;
    private _connections;
    constructor(args: {
        channel: SkyWayChannelImpl;
        name?: string;
        id: string;
        metadata?: string;
        plugin: UnknownPlugin;
        subtype: string;
    });
    /**@private */
    _getConnection(localPersonId: string): UnknownConnection | undefined;
    /**@private */
    _getOrCreateConnection(localPersonId: string): UnknownConnection;
    private _createConnection;
}
//# sourceMappingURL=member.d.ts.map