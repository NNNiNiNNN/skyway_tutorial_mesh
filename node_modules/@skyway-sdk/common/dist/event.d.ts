export declare class Event<T extends any> {
    private _onSetListener;
    private _stack;
    private _eventIndex;
    constructor(_onSetListener?: () => void);
    emit: (arg: T) => void;
    removeAllListeners: () => void;
    pipe: (event: Event<T>) => {
        removeListener: () => void;
    };
    /**
     * イベントが起きた時に実行する関数を登録する。
     * 戻り値として関数の登録を解除する関数が帰ってくる
     */
    add: (callback: (args: T) => void) => {
        removeListener: () => void;
    };
    /**イベントが起きた時に一度だけ実行される関数を登録する */
    once: (callback: (arg: T) => void) => void;
    /**
     * イベントが起きた時に Promise が resolve される
     * @param timeLimit ms
     */
    asPromise: (timeLimit?: number | undefined) => Promise<T>;
    /**
     * イベントが起きた時に実行される boolean を返す関数を登録する。
     * 登録した関数が true を返した時に Promise が resolve される
     * */
    watch: (callback: (arg: T) => boolean | undefined | null, timeLimit?: number | undefined) => Promise<T>;
    get length(): number;
}
export declare class Events {
    events: Event<any>[];
    make<T extends any>(): Event<T>;
    dispose(): void;
}
//# sourceMappingURL=event.d.ts.map