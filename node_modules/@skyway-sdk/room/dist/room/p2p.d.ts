import { SkyWayChannelImpl } from '@skyway-sdk/core';
import { LocalP2PRoomMember, LocalP2PRoomMemberImpl } from '../member/local/p2p';
import { Room, RoomImpl, RoomMemberInit } from './base';
export interface P2PRoom extends Room {
    join: (memberInit?: RoomMemberInit) => Promise<LocalP2PRoomMember>;
}
/**@internal */
export declare class P2PRoomImpl extends RoomImpl implements P2PRoom {
    constructor(channel: SkyWayChannelImpl);
    protected setChannelState(): void;
    protected setChannelListener(): void;
    private _handleOnMemberJoin;
    private _handleOnMemberLeft;
    private _handleOnStreamPublish;
    private _handleOnStreamUnpublish;
    private _handleOnPublicationMetadataUpdate;
    private _handleOnStreamSubscribe;
    private _handleOnStreamUnsubscribe;
    join(memberInit?: RoomMemberInit): Promise<LocalP2PRoomMemberImpl>;
}
//# sourceMappingURL=p2p.d.ts.map